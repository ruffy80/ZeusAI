{
  "buildCommand": "npm --prefix UNICORN_FINAL/client install --no-audit --no-fund && CI=false npm --prefix UNICORN_FINAL/client run build",
  "outputDirectory": "UNICORN_FINAL/client/build",
  "framework": "create-react-app",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

# OpenAPI Schema

This directory contains the forward-only implementation for OpenAPI schema exposure (C12).

- All public endpoints are described in `openapi.yaml`.
- Used for documentation, client generation, and API validation.

Entry: `openapi.yaml` (to be created).
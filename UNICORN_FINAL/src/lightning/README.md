# Lightning Integration

This directory contains the forward-only implementation for Lightning payments (C11).

- Integrates with a Lightning node (LND or Core Lightning) via REST/gRPC.
- Used for instant BTC payments, invoice generation, and webhook settlement.

Entry: `lightning.js` (to be created).
# 🔐 SECRETS SETUP – Zeus AI Unicorn Platform

All secrets must be configured in **GitHub → Settings → Secrets and variables → Actions** before deployment will work.

---

## 📋 Required Secrets

Go to: `https://github.com/ruffy80/ZeusAI/settings/secrets/actions`

Click **"New repository secret"** for each one below:

### 🖥️ Hetzner (VPS Deployment)

| Secret Name | Description | Example |
|---|---|---|
| `HETZNER_HOST` | IP address of your Hetzner server | `65.109.xxx.xxx` |
| `HETZNER_USER` | SSH username | `root` |
| `HETZNER_SSH_PRIVATE_KEY` | Full content of your SSH private key (id_rsa) | `-----BEGIN OPENSSH PRIVATE KEY-----...` |
| `HETZNER_DEPLOY_PATH` | Path on server where app is deployed | `/opt/unicorn` |

### 🚀 Vercel (Frontend Deployment)

| Secret Name | Description | How to get |
|---|---|---|
| `VERCEL_TOKEN` | Vercel API token | [vercel.com/account/tokens](https://vercel.com/account/tokens) |
| `VERCEL_ORG_ID` | Your Vercel team/org ID | Run `vercel link` locally, check `.vercel/project.json` |
| `VERCEL_PROJECT_ID` | Your Vercel project ID | Same as above |

### 🐙 GitHub

| Secret Name | Description |
|---|---|
| `GITHUB_TOKEN` | Automatically provided by GitHub Actions – no action needed |
| `GIT_REPO_URL` | Full HTTPS URL of this repo: `https://github.com/ruffy80/ZeusAI` |

### 🌐 Domain & DNS

| Secret Name | Description | Value |
|---|---|---|
| `DOMAIN` | Your primary domain | `zeusai.pro` |
| `SAV_API_TOKEN` | Sav.com API token for DNS automation | From your Sav.com account |

### 🔑 Application Security

| Secret Name | Description | Recommended value |
|---|---|---|
| `ADMIN_SECRET` | Secret header for admin API access | Random 32+ char string |
| `ADMIN_MASTER_PASSWORD` | Master admin password | Strong password |
| `JWT_SECRET` | JWT signing secret | Random 64+ char string |
| `WEBHOOK_SECRET` | GitHub webhook signature secret | Random 32+ char string |

### ₿ Bitcoin

| Secret Name | Description | Value |
|---|---|---|
| `BTC_WALLET_ADDRESS` | BTC wallet for all payments | `bc1q4f7e66z87mdfj56kz0dj5hvcnpmh0qh4wuv22e` |

### 🤖 AI APIs (Optional but recommended)

| Secret Name | Description |
|---|---|
| `OPENAI_API_KEY` | OpenAI API key for GPT-4 features |
| `DEEPSEEK_API_KEY` | DeepSeek API key (cheaper alternative) |

### 💳 Payments (Optional)

| Secret Name | Description |
|---|---|
| `STRIPE_SECRET_KEY` | Stripe secret key for card payments |
| `STRIPE_PUBLISHABLE_KEY` | Stripe publishable key |
| `STRIPE_WEBHOOK_SECRET` | Stripe webhook signing secret |

### 📧 Email (Optional)

| Secret Name | Description |
|---|---|
| `SMTP_HOST` | SMTP server hostname |
| `SMTP_USER` | SMTP username/email |
| `SMTP_PASS` | SMTP password |

---

## 🚀 After Setting All Secrets

1. Go to **Actions** tab in GitHub
2. Click on **"Vercel Deploy + Hetzner Sync"** workflow
3. Click **"Run workflow"** → select `main` branch → click **"Run workflow"**
4. Monitor the workflow run for any errors

---

## 🌐 Expected URLs After Deployment

- **Vercel (Frontend):** `https://zeusai.pro` or `https://zeusai-[hash].vercel.app`
- **Hetzner (Backend API):** `https://zeusai.pro/api/*`
- **Health Check:** `https://zeusai.pro/api/health`
- **Admin Dashboard:** `https://zeusai.pro/admin`
- **Executive Dashboard:** `https://zeusai.pro/executive`

---

## 🔧 Minimum Required Secrets to Start

For the **absolute minimum** working deployment (Vercel only):
1. `VERCEL_TOKEN`
2. `VERCEL_ORG_ID`
3. `VERCEL_PROJECT_ID`
4. `ADMIN_SECRET`
5. `JWT_SECRET`

Everything else enhances functionality but is optional for initial launch.

---

*Generated by Zeus AI Unicorn Platform – autonomous deployment system*

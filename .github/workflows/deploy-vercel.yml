name: Deploy → Vercel

# Deprecated in Hetzner-only mode.
# Kept only for compatibility/manual visibility.

on:
  workflow_dispatch:

permissions:
  contents: read
  deployments: write

jobs:
  deprecated:
    name: ℹ️ Vercel disabled
    runs-on: ubuntu-latest
    steps:
      - name: Notify
        run: |
          echo "Vercel deployment is disabled."
          echo "Use deploy-hetzner.yml for autonomous production deployments."
